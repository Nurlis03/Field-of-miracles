CREATE TABLE IF NOT EXISTS game_history (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   user_id BIGINT,
   question_id BIGINT,
   answer_status VARCHAR(255),
   user_answer VARCHAR(255),
   time_progress TIMESTAMP WITHOUT TIME ZONE,
   CONSTRAINT pk_gamehistory PRIMARY KEY (id)
);

ALTER TABLE game_history ADD CONSTRAINT FK_GAMEHISTORY_ON_QUESTION FOREIGN KEY (question_id) REFERENCES question (id);

ALTER TABLE game_history ADD CONSTRAINT FK_GAMEHISTORY_ON_USER FOREIGN KEY (user_id) REFERENCES app_user (id);